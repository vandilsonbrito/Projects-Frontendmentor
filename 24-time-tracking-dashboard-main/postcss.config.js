module.exports = {
    plugins: [
      require('tailwindcss'),
      require('autoprefixer'),
      /* require('postcss-assets-hash')({
        basePath: './',
        hashLength: 8,
        assetNameRegExp: /\.(png|jpe?g|gif|svg|woff2?|ttf|eot|map)$/i,
      }), */
    ],
  }